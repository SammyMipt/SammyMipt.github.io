window.BLOCK(String.raw`
---
id: radius-vektor
type: концепт
title: Радиус-вектор и координаты
links: [materialnaya-tochka, sistema-otscheta, ris-radius-vektor, peremeshchenie]
---

Как задать положение материальной точки в пространстве? Для этого используется *радиус-вектор* — вектор, соединяющий начало координат с положением точки. Он является основной величиной, определяющей положение точки в выбранной системе координат.

В декартовой системе координат радиус-вектор обычно обозначается символом $\vec{r}$. Если начало координат находится в точке $O$, а точка $A$ имеет координаты $(x, y, z)$, то радиус-вектор точки $A$ записывается как

$$#dekartova
\vec{r} = x \hat{i} + y \hat{j} + z \hat{k}
$$

где $\hat{i}, \hat{j}, \hat{k}$ — единичные векторы вдоль осей $x, y, z$ соответственно.

Радиус-вектор можно также представить в виде столбца координат, что удобно для дальнейших вычислений:

$$#stolbec
\vec{r} = \begin{bmatrix} x \\ y \\ z \end{bmatrix}
$$

В дальнейшем используется именно такое представление радиус-вектора: оно упрощает запись и расчёты.

Вид радиус-вектора зависит от размерности задачи:

- Точка движется по оси $x$. Радиус-вектор имеет вид $\vec{r} = x(t) \hat{i}$, где $x(t)$ — зависимость координаты от времени.
- Точка движется по плоскости. В этом случае радиус-вектор будет $\vec{r} = x(t) \hat{i} + y(t) \hat{j}$.
- Трёхмерное движение, например движение самолёта. Радиус-вектор выражается как $\vec{r} = x(t) \hat{i} + y(t) \hat{j} + z(t) \hat{k}$.
`);
