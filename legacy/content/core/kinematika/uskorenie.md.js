/* ДОБАВЛЕНО К TEX (03_5_acceleration.tex): формула a = dv/dt (якорь
   proizvodnaya) в конспекте проговорена словами («если мы берем производную
   от скорости по времени, мы получаем ускорение»), но не записана формулой —
   здесь записана явно. Разговорные обороты конспекта сняты по правилам
   учебникового регистра, список — в CONVERSION-LOG.md. */
window.BLOCK(String.raw`
---
id: uskorenie
type: вывод
title: Ускорение: как меняется скорость
links: [skorost, normalnoe-i-tangencialnoe]
---

Скорость описывает, как меняется положение объекта со временем. А как меняется сама скорость? За это отвечает ускорение — ещё один шаг в той же логике. Снова рассматривается изменение некоторой величины с течением времени, но теперь это не положение точки, а её скорость. Если скорость объекта меняется, значит, у него есть ускорение.

Ускорение выражается точно так же, как скорость, только вместо перемещения рассматривается изменение скорости $\Delta \vec{v}$ за промежуток времени $\Delta t$:

$$#opredelenie
\vec{a} = \lim_{\Delta t \to 0} \frac{\Delta \vec{v}}{\Delta t}
$$

Ускорение — это то, как скорость меняется с течением времени. При уменьшении промежутка времени $\Delta t$ до очень маленького получается мгновенное ускорение, и предел записывается как производная скорости по времени:

$$#proizvodnaya
\vec{a} = \frac{d\vec{v}}{dt}
$$

Ускорение проще понять через аналогию с перемещением и скоростью: производная радиус-вектора по времени давала скорость; производная скорости по времени даёт ускорение. Ускорение показывает, как быстро скорость увеличивается или уменьшается. Если скорость — это темп, с которым меняется положение, то ускорение — это темп, с которым меняется сама скорость.
`);
