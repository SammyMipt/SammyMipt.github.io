window.BLOCK(String.raw`
---
id: moment-sily
type: вывод
title: Момент силы и уравнение моментов
links: [moment-impulsa, moment-vnutrennih-sil, vtoroj-zakon]
---

Чем меняется момент импульса? Логика введения момента силы повторяет логику момента импульса. Рассмотрим радиус-вектор $\vec{r}$ и вектор силы $\vec{F}$. По аналогии с моментом импульса момент силы определяется как их векторное произведение:

$$#opredelenie
\vec{M} = \vec{r} \times \vec{F}
$$

Выведем закон изменения момента импульса. Возьмём определение $\vec{L} = \vec{r} \times \vec{p}$ и продифференцируем его по времени. Получаются два слагаемых:

$$#proizvodnaya
\frac{d\vec{L}}{dt} = \frac{d}{dt}(\vec{r} \times \vec{p}) = \dot{\vec{r}} \times \vec{p} + \vec{r} \times \dot{\vec{p}}
$$

где $\dot{\vec{r}}$ — производная радиус-вектора по времени, а $\dot{\vec{p}}$ — производная импульса по времени. Порядок сомножителей в векторных произведениях здесь важен: векторное произведение антикоммутативно, и правило дифференцирования произведения сохраняет каждый вектор на своём месте. Строго это проверяется через представление векторного произведения определителем.

Первое слагаемое равно нулю: $\dot{\vec{r}}$ — это скорость, а $\vec{p} = m \vec{v}$, значит, векторы $\vec{p}$ и $\dot{\vec{r}}$ коллинеарны:

$$#pervyj-nol
\dot{\vec{r}} \times \vec{p} = 0
$$

Остаётся только второе слагаемое:

$$#vtoroj-chlen
\frac{d\vec{L}}{dt} = \vec{r} \times \dot{\vec{p}}
$$

Производная импульса $\dot{\vec{p}}$ по второму закону Ньютона ([[vtoroj-zakon]]) — это сила, действующая на материальную точку: $\dot{\vec{p}} = \vec{F}$. Получаем:

$$#uravnenie
\frac{d\vec{L}}{dt} = \vec{r} \times \vec{F} = \vec{M}
$$

Это уравнение изменения момента импульса: момент импульса материальной точки меняется только за счёт момента внешних сил.

Соотнесём результат с классическим представлением о моментах, знакомым по школьному рычагу. Неподвижная точка, вокруг которой поворачивается рычаг, разделяет направления вращения на «по часовой стрелке» и «против часовой стрелки». Это интуитивное представление сохраняется, но теперь оно обобщено векторным произведением. Момент силы $\vec{M} = \vec{r} \times \vec{F}$ — вектор, и его направление даёт правило правой руки: если сила поворачивает объект по часовой стрелке, момент направлен «вглубь» плоскости, перпендикулярно ей; при повороте против часовой стрелки — «наружу».

Уравнение моментов в краткой записи:

$$#kratko
\frac{d\vec{L}}{dt} = \vec{M}
$$

Можно ввести и плечо силы $\vec{r}_\perp$ — перпендикуляр от полюса до линии действия силы. Именно плечо участвует в формировании момента: смещение точки приложения силы вдоль её линии действия не меняет расчёт моментов. Это свойство позволяет применять момент силы к анализу механических систем, а не только отдельных точек.
`);
