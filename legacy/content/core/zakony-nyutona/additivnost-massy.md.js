window.BLOCK(String.raw`
---
id: additivnost-massy
type: вывод
title: Аддитивность инерционной массы
links: [massa-i-impuls, princip-otnositelnosti, centr-mass]
---

Масса определена как коэффициент, на который нужно умножить скорость, чтобы получить количество движения ([[massa-i-impuls]]). Что происходит с массой, если взять два тела и объединить их? Бытовой опыт подсказывает, что массы этих тел просто складываются. Проверим, следует ли это из уже установленных законов.

Рассмотрим набор материальных точек $A_1, A_2, A_3, \ldots$, которые вместе представляют систему $A$. В некоторой системе отсчёта $S$ суммарный импульс всех этих частиц равен импульсу системы:

$$#impuls-sistemy
\sum_i \vec{p}_i = \vec{p}
$$

или, выразив через массу и скорость,

$$#cherez-skorosti
\sum_i m_i \vec{v}_i = M\vec{v}
$$

где $M$ — общая масса системы, а $\vec{v}$ — скорость системы.

Перейдём в другую систему отсчёта $S'$, которая движется относительно $S$ со скоростью $\vec{V}$. Тогда в системе $S'$ суммарный импульс записывается как $\sum_i \vec{p}_i' = \vec{p}'$, то есть:

$$#shtrih
\sum_i m_i \vec{v}_i' = M\vec{v}'
$$

По правилу сложения скоростей из [[princip-otnositelnosti]] $\vec{v}_i' = \vec{v}_i - \vec{V}$ и $\vec{v}' = \vec{v} - \vec{V}$. Подставим эти выражения в уравнение для системы $S'$:

$$#podstanovka
\sum_i m_i (\vec{v}_i - \vec{V}) = M(\vec{v} - \vec{V})
$$

Раскрывая скобки, получим:

$$#raskrytie
\sum_i m_i \vec{v}_i - \vec{V} \sum_i m_i = M\vec{v} - M\vec{V}
$$

Первые слагаемые слева и справа равны между собой: это уравнение импульса в исходной системе $S$. Значит, равны и оставшиеся слагаемые, $\vec{V} \sum_i m_i = M\vec{V}$, откуда:

$$#itog
M = \sum_i m_i
$$

Из принципа относительности и закона сохранения импульса следует, что масса является аддитивной величиной: масса всей системы равна сумме масс её частей.
`);
