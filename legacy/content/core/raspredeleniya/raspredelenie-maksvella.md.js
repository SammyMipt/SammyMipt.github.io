/* ДОБАВЛЕНО К TEX (11_7_maxwell_disribution.tex): шаг вывода
   дифференциального уравнения выписан явно — в конспекте «применяя
   условие изотропности, получаем дифференциальное уравнение» без
   самого уравнения (WRITING §2.3); замечание о гауссовой форме
   покомпонентного распределения — связка с вопросом 10 методички. */
window.BLOCK(String.raw`
---
id: raspredelenie-maksvella
type: вывод
title: Распределение Максвелла по компонентам скорости
links: [statisticheskij-integral, ris-elementarnyj-obem, raspredelenie-po-modulyu, raspredelenie-gaussa]
---

Как распределены молекулы идеального газа по скоростям? Средняя скорость известна через кинетическую энергию, но в реальном газе молекулы движутся с самыми разными скоростями, и нужен закон, по которому эти скорости распределены.

Введём функцию распределения $f(\vec{v})$: вероятность найти молекулу со скоростью, компоненты которой лежат в интервалах $[v_x, v_x + dv_x]$, $[v_y, v_y + dv_y]$, $[v_z, v_z + dv_z]$, равна

$$#dolya
\frac{dn}{n} = f(\vec{v})dv_xdv_ydv_z
$$

где $n$ — концентрация молекул, $dn$ — концентрация молекул со скоростями из указанного элементарного объёма пространства скоростей (см. [[ris-elementarnyj-obem]]).

Пространство изотропно: все направления равноправны, и $f(\vec{v})$ может зависеть только от модуля скорости. Движения по разным осям независимы, поэтому функция распадается на произведение одинаковых множителей:

$$#proizvedenie-komponent
f(\vec{v}) = \phi(v_x)\phi(v_y)\phi(v_z)
$$

где $\phi$ — одна и та же функция для всех компонент. Логарифмируем:

$$#logarifm
\ln f(\vec{v}) = \ln \phi(v_x) + \ln \phi(v_y) + \ln \phi(v_z)
$$

Модуль скорости связан с компонентами:

$$#modul
v^2 = v_x^2 + v_y^2 + v_z^2
$$

Продифференцируем равенство логарифмов по $v_x$ при фиксированных $v_y$ и $v_z$. Левая часть зависит от $v_x$ только через $v^2$, и по цепному правилу производная равна $2v_x$, умноженному на производную $\ln f$ по $v^2$. Правая часть — производная $\ln \phi(v_x)$, она зависит только от $v_x$. Равенство возможно, лишь когда производная $\ln f$ по $v^2$ — константа; обозначим её $-\alpha$:

$$#uravnenie
\frac{d \ln \phi(v_x)}{d v_x} = -2\alpha v_x
$$

Интегрирование даёт гауссову экспоненту для каждой компоненты:

$$#komponenta
\phi(v_i) = A \exp(-\alpha v_i^2)
$$

где $A$ и $\alpha$ — константы, которые предстоит определить. Их дают два условия. Первое — нормировка: полная вероятность найти молекулу с какой-либо скоростью равна единице. Второе — связь средней кинетической энергии с температурой:

$$#ravnoraspredelenie
\left\langle\frac{mv_i^2}{2}\right\rangle = \frac{kT}{2}
$$

где $m$ — масса молекулы. Из этих условий:

$$#konstanty
\alpha = \frac{m}{2kT}, \quad A = \left(\frac{m}{2\pi kT}\right)^{1/2}
$$

По каждой компоненте скорости получилось гауссово распределение из [[raspredelenie-gaussa]] с нулевым средним: движение вправо и влево равновероятно, а ширина колокола растёт с температурой.
`);
